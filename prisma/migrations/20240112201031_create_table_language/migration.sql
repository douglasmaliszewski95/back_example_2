BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[language] (
    [languageId] NVARCHAR(1000) NOT NULL,
    [name] NVARCHAR(1000) NOT NULL,
    CONSTRAINT [language_pkey] PRIMARY KEY CLUSTERED ([languageId])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
